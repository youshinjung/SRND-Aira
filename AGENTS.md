# AGENTS.md — SRND-Aira 코딩 에이전트 지침

이 파일은 Codex, Claude Code 및 다른 코딩 에이전트가 가장 먼저 따라야 하는 프로젝트 정본입니다.

## 프로젝트 범위

- 구현 대상은 `docs/FEATURES.md`의 15개 기능뿐이다.
- 기존 프로젝트, 기존 대시보드, 과거 완성 기능을 가져오거나 참고 코드로 복사하지 않는다.
- 기능 하나를 시작할 때 `templates/feature/`의 네 파일을 `src/features/<번호>-<기능명>/`에 복사한다.
- 템플릿 원본은 수정하지 않는다.
- 기능 폴더의 최종 파일명은 `extract.js`, `fill.js`, `record.md`, `sample.json`으로 고정한다.
- `real-data.json` 및 이에 준하는 실제 데이터 파일은 만들지 않는다.

## 작업 시작 전

1. 저장소 루트에서 `git status`를 확인한다.
2. 대상 기능의 `STATUS.md`와 `docs/FEATURES.md`를 읽는다.
3. `docs/WORKFLOW.md`와 `docs/SECURITY.md`를 따른다.
4. 대상 기능과 무관한 파일 및 다른 기능 폴더를 수정하지 않는다.

## 화면 조사 원칙

- 교육영상은 흐름을 이해하는 자료일 뿐이다. 실제 선택자, 컴포넌트 ID, 데이터셋과 분기는 실제 SRnD 화면에서 확인한다.
- 사용자의 외부 Google Chrome과 터미널을 사용한다. 사용자가 변경을 요청하지 않는 한 인앱 브라우저로 전환하지 않는다.
- 로그인과 위임계정 전환은 사용자가 직접 한다.
- 모든 관련 분기, 팝업, 조건부 필드, 조회 결과, 행 선택과 최종 준비 상태를 기록한다.
- 동명이인이나 복수 결과처럼 판단이 필요한 선택은 자동 선택하지 않고 사용자에게 선택을 요청한다.
- 테스트에 필요한 실제 조회값은 실행 중 일시적으로 입력받을 수 있지만 파일, 로그, 샘플 또는 기록 문서에 보존하지 않는다.

## 절대 금지 동작

- `저장`, `신청`, `변경신청`, `발급신청`, `제출`, `청구`, `승인`, `삭제` 및 같은 효력을 가진 최종 버튼을 자동으로 누르지 않는다.
- 비밀번호, API 키, 로그인 정보, 주민등록정보, 계좌번호, 실제 카드번호 또는 실제 개인정보를 기록하거나 커밋하지 않는다.
- 실제 데이터에서 추출한 JSON 파일을 만들거나 저장소에 추가하지 않는다.
- 화면 기록 없이 선택자나 Nexacro 데이터셋을 추측하여 구현하지 않는다.

## 구현 및 검증 순서

1. 영상과 사용자 설명으로 업무 흐름을 정리한다.
2. 외부 Chrome에서 실제 클릭 경로와 화면 구조를 기록한다.
3. 기능 스키마와 자동/수동 경계를 `record.md`에 먼저 정리한다.
4. 완전히 가상의 값만 사용해 `sample.json`을 만든다.
5. 테스트용 CMD와 임시 도구는 저장소의 `.work/`에서 만들고 Git에 포함하지 않는다.
6. 사용자에게 테스트 CMD 명령을 먼저 제공하고, 터미널 로그를 받아 반복 수정한다.
7. 사용자가 테스트 완료를 확인한 후 `extract.js`, `fill.js`, `record.md`, `sample.json`을 최종 점검한다.
8. `STATUS.md`를 `구현완료`로 바꾸기 전에 문법 검사, JSON 파싱 검사와 금지 버튼 검사를 수행한다.

## Windows 명령

- `npm` 대신 `npm.cmd`를 사용한다.
- `npx` 대신 `npx.cmd`를 사용한다.
- 한글 때문에 CMD가 깨지면 CMD 자체는 ASCII와 CRLF로 만들고 한글 안내는 Node.js가 출력하게 한다.

## Git 안전 규칙

- push 요청을 받으면 먼저 `git status`와 실제 변경 파일을 확인한다.
- `.env`, 실제 증빙, 실제 데이터, 브라우저 프로필, 스냅샷·탐색 덤프가 포함되면 커밋하지 않는다.
- 공용 문서나 템플릿 변경은 그 영향을 사용자에게 먼저 알린다.
- 충돌이 발생하면 임의로 덮어쓰거나 리셋하지 말고 사용자에게 상황을 설명한다.
